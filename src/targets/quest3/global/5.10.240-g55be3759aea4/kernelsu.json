{
  "$comment": [
    "The KernelSU late-load build for this target. A run of it reached a loaded,",
    "answering KernelSU on the device: module Live, /data/adb/ksud installed,",
    "`su` returning uid 0 in u:r:ksu:s0, and SELinux back to Enforcing.",
    "",
    "ddkImage below is NOT what produced that module -- kernelSource is.",
    "The generic android12-5.10 DDK is only an approximation of",
    "this kernel -- Meta builds its own rather than shipping GKI -- and a module",
    "built from it rebooted the device on load every time, three runs out of",
    "three, while resolving every symbol. CFI, LTO, SCS and PAC all match; what",
    "does not is 104 options that are =y on the device and absent from the DDK,",
    "four of them guarding task_struct fields (KASAN, PREEMPT_NOTIFIERS,",
    "TASK_DELAY_ACCT, and UBSAN_TRAP compiling traps into the module).",
    "",
    "What produced the module that works: Meta's own source for this build,",
    "",
    "    kernel/oculus-linux-kernel, branch oculus-quest3-kernel-master,",
    "    HEAD 'Oculus Quest 3 kernel project build: 5234532.3540.520'",
    "    = ro.build.version.incremental 52345320035400520",
    "",
    "with the device's own /proc/config.gz as .config, built with the DDK",
    "image's toolchain (its Android clang r416183b builds 5.10; the host's",
    "clang-21 does not) and used as KDIR for an external module build. A full",
    "kernel build is not needed and does not finish here -- Qualcomm's vendor",
    "drivers fail on tracepoint includes -- but modules_prepare alone is not",
    "enough either: KernelSU's selinux part needs the generated",
    "security/selinux/flask.h, which the failed full build produces before it",
    "stops.",
    "",
    "That build is kernelSource below, run by the kernel-module workflow in",
    "this repository rather than by the payload build, because it takes ten",
    "minutes on a tree that never changes. This entry references its published",
    "result -- see prebuiltModule. ddkImage still names the closest KMI, which",
    "is what ksud is built with and what says which KMI this is; the module",
    "does not come from it.",
    "",
    "Measured on the device and worth keeping:",
    "  - CONFIG_MODULES=y and CONFIG_MODULE_SIG unset, so unsigned modules load.",
    "  - CONFIG_MODVERSIONS=y, answered as everywhere else here by an empty",
    "    Module.symvers and a zero-length __versions section, with ksud",
    "    resolving the 48 unexported symbols from /proc/kallsyms.",
    "  - kptr_restrict is 2, which hides every kallsyms address from root as",
    "    well; the bootstrap helper now sets 0 across the load and restores it.",
    "  - The audit decides the KMI, not the ship date:",
    "    extract_target_symvers.py against a vmlinux recovered from the running",
    "    kernel, then audit_module_against_target.py. MISSING_SYMBOL is fatal,",
    "    MISSING_EXPORT is not. android14-6.1 gives 10, android12-5.10 gives 1,",
    "    the real source gives 0.",
    "",
    "devices/quest3 is upstream 953b403a backported onto the pinned 32525. It",
    "keeps the tracepoint mark on /system/bin/stub_zygote, which is how Horizon",
    "OS starts the zygote; without it a `ksud soft-reboot` brings back a zygote",
    "no KernelSU hook fires for, and the manager dies with SIGSYS on its first",
    "supercall. The set exists at 32514 and 32525 and deliberately not at",
    "32567, which already carries the commit: a pin move to it fails this build",
    "until the name is taken out of patchSets, which is the reminder wanted."
  ],
  "id": "quest3",
  "kmi": "android12-5.10",
  "ddkImage": "ghcr.io/ylarod/ddk-min:android12-5.10-20260313",
  "kernelRelease": "5.10.240-g55be3759aea4",
  "patchSets": [],
  "config": [],
  "kernelSource": {
    "$comment": [
      "The kernel this module is built against: Meta's own source for this",
      "build, not the ddkImage above. The commit, not the branch -- a branch",
      "that moves is a different kernel. Its subject is the build the device",
      "runs, which is how it was found:",
      "",
      "    Oculus Quest 3 kernel project build: 5234532.3540.520",
      "    = ro.build.version.incremental 52345320035400520",
      "",
      "clang is a directory inside the ddkImage. The toolchain comes from the",
      "image even though the kernel does not: it carries an Android clang that",
      "builds 5.10, which a current host clang does not. Nothing else in the",
      "image is used for this build.",
      "",
      "The .config is kernel.config beside this file: the device's own",
      "/proc/config.gz, decompressed so it can be read and diffed, and not a",
      "defconfig -- being close to what the device runs is the entire point of",
      "building from source here."
    ],
    "repo": "https://github.com/facebookincubator/oculus-linux-kernel.git",
    "ref": "oculus-quest3-kernel-master",
    "commit": "bbb8e0cff7f048bdf011ab3e7fd686886879d80f",
    "subject": "Oculus Quest 3 kernel project build: 5234532.3540.520",
    "clang": "clang-r416183b"
  },
  "prebuiltModule": {
    "$comment": [
      "The published result of building kernelSource above. The generic",
      "android12-5.10 DDK resolves every symbol and still reboots this device",
      "on load, three runs out of three; the image is named only so the entry",
      "says which KMI this is and so the toolchain that builds ksud is the one",
      "everything else uses.",
      "",
      "The digest is what makes this a reference to a build rather than trust",
      "in a URL, and the action checks vermagic, __versions, the manager",
      "package and the manager certificate on top of it -- the same things it",
      "proves about a module it built itself.",
      "",
      "Both of those last two are strings the compiler keeps, and the",
      "certificate one is worth more than it looks. EXPECTED_HASH is only",
      "compared if the certificate is short enough to read, and the size is a",
      "constant the compiler can fold: with a certificate over CERT_MAX_LENGTH",
      "it proves the branch dead and drops the strcmp and its literal. So the",
      "hash being present in the module is exactly the statement that a manager",
      "signed with it can be recognised at all. It was absent for every build",
      "made while the key was RSA-4096."
    ],
    "url": "https://github.com/WitAqua-tools/Root-My-Device-Payloads/releases/download/modules-20260803-2aa87cc/kernelsu-quest3.ko",
    "sha256": "39933d93a871131383336c60a61a950aa277634f6d650a65243397a3c8de2877"
  }
}
